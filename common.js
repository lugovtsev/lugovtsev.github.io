"use strict";

$(document).ready(function() {
  /*$('button').click(function() {
    if ($('#input-add').val() != '')
    $('.list ul').append('<li>' + $('#input-add').val()
                         + ' <span id="del">-</span></li>');
    $('#input-add').val('');
  });

  $('body').on('click', '#del', function() {
    $(this).parent().css('text-decoration', 'line-through');
  });*/
});
